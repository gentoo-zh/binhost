[Unit]
Description=每日运行 binhost 健康检查

# 镜像机那份走 cron，跑在 00:20 UTC。这一份错开十二小时：两台查的是同一组
# 目标，同一分钟跑等于一次故障收两条通知，错开之后一天有两次独立观察。
[Timer]
# 排在构建之后：构建 16:00 起跑约两个半小时，20:20 查到的是刚发布的结果。
OnCalendar=*-*-* 20:20:00 Asia/Shanghai
Persistent=true

[Install]
WantedBy=timers.target
