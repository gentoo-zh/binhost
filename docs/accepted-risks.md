# 已知风险与延后事项

本文分为两类。**延后事项**记录已经确认但尚未完成的工作，并注明负责人、解除条件
和期限；满足解除条件前不得删除。**接受的风险**记录经权衡后不处理的事项；若调整决定，
应移动对应条目，并在提交中说明理由。

两类都不表示问题已经修复。站点与 README 的表述必须与本文一致。

## 延后事项

本节负责人：Zakk。

### 依赖闭包不执行完整解析

解除条件：接入 Portage 解析器，或提交可审查的证明，明确索引匹配与
解析器结论一致的边界。期限：私钥拆分之后。

`build/stage-index.py` 按索引自身匹配原子，不执行解析器。`||` 组取第一个索引能完整
满足的分支，没有这样的分支时整组不发，并记进 `unresolved.txt`。

`build/verify-deps.py` 使用 emerge 前的基础系统快照，并按版本、slot、USE 与 repository
匹配完整 Atom。检查还使用当轮 Gentoo binhost 索引的最小快照；剩余的无 USE 约束
原子可以由当前 Gentoo 与 gentoo-zh 源码仓库中的可见版本满足。一般源码包的
USE 配置未知，
因此带 USE 约束的原子不会由源码快照兜底。对于 `acct-group/*`、`acct-user/*` 与
`virtual/*`，源码快照按当前 ebuild 的 `IUSE` 默认启用项匹配完整 Atom。四个集合都
不满足的原子一律判不通过，但这不等于执行过完整解析器。

在此之前，站点与 README 只能说本站不替代 Gentoo 官方 binhost，不得声称依赖完整。

### 立即隔离后旧索引可能返回 404

解除条件：改用代际目录并进行单次切换，或提出其他发布顺序，并证明其
同时满足立即下架与可用性。期限：与代际切换一并处理。

`quarantine.txt` 只列出设置 `RESTRICT=bindist`、许可证不允许再分发或无法确认再分发
资格的缓存产物。源码本地安装、mask、排除与版本移除不进入该清单，而在新索引
切换后退役。

`build/publish.sh` 在替换索引之前先删除 `quarantine.txt` 列出的产物。因为合规下架
优先于可用性，所以顺序是有意的。代价是随后的索引传输若失败，旧索引仍指向已删除的
文件，下游会返回 404，直到下一轮构建。目前仅在移除失败时中止整轮发布。

## 接受的风险

### 同代文件的替换不是一次原子切换

`build/publish.sh` 先备份旧的 `Packages`、`Packages.gz`、`installed.txt`、
`official.txt`、`source.txt` 与 `generation.json`。任一替换失败时会还原六个文件。
仍然存在的是：逐项 rename 期间进程被杀或主机重启，可能留下不同代；还原用的 `mv`
失败时没有第二层补救。

接受的理由是完整解法需要改镜像机的目录结构与 nginx 配置。每日检查会验证
`generation.json`，遇到混合代时停止依赖验证并告警。

### 站点同步不是一次原子切换

`deploy/publish-site.sh` 执行一次 rsync，并使用 `--delay-updates` 与 `--delete-delay`。
接收与删除都延迟到传输结束，但 rename 阶段仍逐个处理文件。中途失败可能留下不同代的
assets 与页面。
接受的理由与上一条相同。

### 镜像机没有远端发布锁

目前只有一台构建机，`cycle.sh` 的本机锁会序列化定时构建与发布。镜像机无法阻止两个
发布者并行写入同一组暂存文件；人工发布前必须确认定时构建未在运行。增加第二台发布者
之前，必须加入远端锁和每轮唯一的暂存名称。
