# Packages that are not carried, with the reason for each.
#
# Mutually exclusive with packages.txt: validate.py rejects any package that
# appears in both. Nothing here is built; the site marks it as not carried and
# shows the reason.
#
# Two kinds belong here. The first is a settled verdict -- a licence, an
# incompatible profile, an upstream that forbids redistribution -- which will
# not resolve on its own. The second is a package that has failed to build
# round after round: say how many rounds were tried, and come back to it once
# an environment-side cause has been ruled out, rather than leaving it here
# forever.
#
# Format: category/package  <TAB>  reason

app-i18n/fcitx-kkc		依赖 libkkc 的 configure 需要 XML::Parser，基础镜像补 perl-cleaner 后复查
app-misc/crush			许可证 FSL-1.1-MIT 不允许再分发
media-gfx/scangearmp		许可证 Canon-IJ 只准搭配 Canon 喷墨机使用，不在 @BINARY-REDISTRIBUTABLE
media-plugins/osdlyrics	configure 需要 XML::Parser，基础镜像补 perl-cleaner 后复查
net-proxy/proxy-server		ebuild 没有 KEYWORDS 声明，任何架构都装不上
sys-kernel/mkinitcpio		需要 dbus[systemd]，与我们的 OpenRC profile 不兼容
sys-kernel/xanmod-kernel	内核配置因人而异，发预编译的等于把我们的配置强加给用户
app-dicts/fcitx-pinyin-moegirl	许可证 CC-BY-NC-SA-3.0 不允许再分发，整体解析会因它中止
